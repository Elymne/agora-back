const boxRoutes = require("./routes")
const Box = require("./box")
const boxController = require("./controller")
const boxServices = require("./services")

module.exports = {
    boxRoutes,
    Box,
    boxController,
    boxServices,
}
